import {useParams} from "react-router-dom";

const PostDetail = () => {
    const param = useParams();

    return (
        <div>
            post detail
        </div>
    )
}

export default PostDetail